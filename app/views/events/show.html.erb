
<div class=" event">

  <p>
    <strong>Name:</strong>
    <%= @event.name %>
  </p>

  <p>
    <strong>Access Code:</strong>
    <%= @event.access_code %>
  </p>


  <p>

    <%= link_to 'Edit', edit_event_path(@event) %> |
    <%= link_to 'Back', events_path %>

    <br>
    <br>
    <%= link_to 'View Attendees', attendees_path(@event), class: "button  w-button" %>

    <%= link_to "Create Ask", new_ask_path(event_id: @event.id), class: "button  w-button" %>
  </p>

    <br>
    <br>
  <div class="asks_count"><h4> <%= @event.asks.count %> <%= "Ask".pluralize(@event.asks.count) %></h4></div>

</div>
<div class="">
  <% if @event.asks.any? %>
  <% @event.asks.each do |ask| %>
  <ol class="asks">
    <%= render ask %>
  </ol>
  <% end %>
  <% end %>
</div>

